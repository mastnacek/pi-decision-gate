# ADR-003: Samostatný plugin — „architecture guardian" jako post-change / turn-bo
- **Date:** 2026-09-18 13:39:11
- **Status:** active
- **Context:** Execute and verify command: ${rawCommand}
      : Execute tool '${event.toolName}' with arguments:\n${rawPreview};

    ctx.ui.notify("Rozděluji okno v Herdr...", "info");
    const split = await spli
- **Decision:** "pi";
  const agentArgs: string[] = [];

  if (kind === "pi") {
    agentArgs.push("--use-theme", input.theme ??
- **Consequences:** |
|---|---|---|
| safety gate (JEV risk/irreversible) | per-action blok | mode/useJev |
| model suitability | per-action advisory | ne |
| thinking recommendation | per-action advisory | ne |
| Herdr
